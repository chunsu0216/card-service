package com.cardservice.repository;

import com.cardservice.entity.ApproveRefuse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ApproveRefuseRepository extends JpaRepository<ApproveRefuse, Long> {
}
